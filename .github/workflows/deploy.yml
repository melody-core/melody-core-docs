name: Deploy docs

on:
  push:
  branches:
    - master

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - name: deploy-docs
      - run: npm install
      - run: npm run deploy
